version: '3'
services:
  sync:
    image: wazopbx/wait
    environment:
      TIMEOUT: "${INTEGRATION_TEST_TIMEOUT}"

  provd:
    image: wazopbx/xivo-provd
    volumes:
      - "../..:/usr/src/xivo-provisioning/"
      - "./etc/xivo/provd/provd.conf:/etc/xivo/provd/provd.conf"
      - "./provd/plugins:/var/lib/xivo-provd/plugins"
    ports:
      - "8666"
      - "8667"
      - "69/udp"

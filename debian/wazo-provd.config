#!/bin/bash

set -e

. /usr/share/debconf/confmodule

db_input medium wazo-provd/db-host || true
db_input medium wazo-provd/db-admin-username || true
db_input medium wazo-provd/db-admin-password || true
db_input medium wazo-provd/db-app-name || true
db_input medium wazo-provd/db-app-username || true
db_input medium wazo-provd/db-app-password || true
db_go || true

db_stop
